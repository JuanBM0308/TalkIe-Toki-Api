spring.application.name=talkie-toki-api

# Database configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST}:5432/db_talkie_tokie
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update

# JPA
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Security JWT secret key
talkie.security.oauth0.jwt.secret-key=${TALKIE_SECRET_JWT:T07MZBoxpsEbxdzJVhoKRRZF036CPR8NN8aD56xFEEJD8fWgpE}